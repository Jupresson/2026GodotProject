# Copilot Instructions

- Use tabs for indentation in all code edits and newly created code files.
- Do not convert existing tab-indented files to spaces.
- Preserve existing indentation style when a file already has mixed formatting, but prefer tabs for all new lines.
- Keep `indent_style = tab` rules in `.editorconfig` aligned with code formatting.
